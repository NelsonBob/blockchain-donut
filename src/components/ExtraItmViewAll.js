import styled from "styled-components";
import Colors from "../assets/Colors";

const ExtraItmViewAll = styled.span`
  text-align: center;
  color: ${Colors.Gray3};
  font-weight: 400;
  font-size: 0.9rem;
  padding: 0.5rem 1rem;
`;

export default ExtraItmViewAll;